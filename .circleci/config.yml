# Java Gradle CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-java/ for more details
#
version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/openjdk:8-jdk

      # Specify service dependencies here if necessary
      # CircleCI maintains a library of pre-built images
      # documented at https://circleci.com/docs/2.0/circleci-images/
      # - image: circleci/postgres:9.4

    working_directory: ~/Ferrilata---Java-Tribes

    environment:
      # Customize the JVM maximum heap limit
      JVM_OPTS: -Xmx3200m
      TERM: dumb

<<<<<<< HEAD
    branches:
      only:
        - development
        - preproduction
        - master

=======
>>>>>>> d048ebbe3e8bc2782557922704e603f5c9b70636
    steps:
      - checkout

      # Download and cache dependencies
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "build.gradle" }}
            # fallback to using the latest cache if no exact match is found
            - v1-dependencies-

      - run: gradle dependencies

      - save_cache:
          paths:
            - ~/.gradle
          key: v1-dependencies-{{ checksum "build.gradle" }}


      # run tests!
      - run: gradle test

  deploy:
    docker:
      - image: buildpack-deps:trusty
<<<<<<< HEAD
      steps:
        - checkout
        - run:
            name: Deploy preproduction to Heroku
            command: |
              git push https://heroku:$HEROKU_API_KEY@git.heroku.com/$HEROKU_APP_NAME.git preproduction
  workflows:
    version: 2
    build-deploy:
      jobs:
        - build
        - deploy:
            requires:
              - build
            filters:
              branches:
                only: preproduction
=======
      
    steps:
      - checkout
      - run:
          name: Deploy preproduction to Heroku
          command: |
            git push https://heroku:$HEROKU_API_KEY@git.heroku.com/$HEROKU_APP_NAME.git preproduction

workflows:
  version: 2
  build-deploy:
    jobs:
      - build:
          filters:
            branches:
              only:
                - development
                - preproduction
                - master
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: preproduction
>>>>>>> d048ebbe3e8bc2782557922704e603f5c9b70636
